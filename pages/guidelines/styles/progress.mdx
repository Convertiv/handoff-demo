---
title: "Mixins: Progress Bars"
description: | 
  Create a progress bar to show mobile users of progress through sliders

weight: 0
image: hero-design
menuTitle: 'Progress Bars'
metaTitle: 'Progress Bars | Handoff Design System'
metaDescription: 'How to use handoff tokens in your project.'
enabled: true
menu: 

---
import { Hero } from "handoff-app/src/app/components/Hero";

<Hero title={props.title} image={false}>
  <p>{props.description}</p>
</Hero>

## Where to Find

- You can find these styles in `integration/snippets/common/progress-bar.scss`.


```scss title="Set color and font family"
.progress-bar__container {
    display: flex;
    align-items: center;
    width: calc(100% - 18px);
    margin: 14px 0 0;

    .progress-bar__inner,
    .progress-bar__wrapper {
        display: block;
        height: 5px;
    }

    .progress-bar__wrapper {
        width: 100%;
        min-width: auto;
        border-radius: 4px;
        background: rgba(color(pure-black), .1);
        overflow: hidden;
    }

    .progress-bar__inner {
        width: 1%;
        transition: width .2s ease-in-out;
        background: rgba(0, 119, 200, 1);
    }

    .progress-bar__slide-count {
        display: block;
        position: relative;
        right: -31px;
        width: 50px;
        font-family: $font-barlow-sans;
        font-size: 16px;
        font-weight: $font-weight-semi-bold;
        line-height: 1.2;
        letter-spacing: 4px;
        color: color(pure-black);
        i {
            display: inline-block;
            width: 3px;
        }
    }
}

.slick-dotted {
    &.slick-slider {
        margin-bottom: 0;
    }
}

.slick-dots {
    display: none !important; // sass-lint:disable-line no-important
}


@include media-breakpoint-up(lg) {
    .progress-bar__container {
        display: none;
    }

    .slick-dotted {
        &.slick-slider {
            margin-bottom: 30px;
        }
    }

    .slick-dots {
        display: flex !important; // sass-lint:disable-line no-important
        align-items: center;
        justify-content: center;
        max-width: 930px;
    }
}

@include media-breakpoint-up(xl) {
    .bs-section--add-progress-bar {
        .slick-dots {
            max-width: 1170px;
        }
    }
}

```